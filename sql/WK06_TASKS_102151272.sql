SELECT M.MOVIENO, M.TITLE, M.RUNTIME, M.TMDB_SCORE, R.LONGDESC
FROM MOVIE1272 M
INNER JOIN RATING1272 R
ON M.RATINGCODE = R.RATINGCODE
WHERE (LONGDESC LIKE '%recommended%' OR '%RECOMMENDED%'
OR (TMDB_SCORE LIKE '%44%')
OR (M.TITLE LIKE '%t%' OR '%T%')

