SELECT M.MOVIENO, M.TITLE,M.RATINGCODE, R.SHORTDESC, M.TMDB_SCORE
FROM MOVIE1272 M
INNER JOIN RATING1272 R
ON M.RATINGCODE = R.RATINGCODE
WHERE RATINGCODE = MA OR RUNTIME >=100 AND <=102
ORDER BY M.MOVIENO ASC;
